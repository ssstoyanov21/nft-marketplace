{
  "name": "nft-backend",
  "version": "1.0.0",
  "scripts": {
    "setup": "hardhat run scripts/deploy-local.js --network localhost && hardhat run scripts/deploy-nft.js --network localhost && hardhat run scripts/mint-and-list.js --network localhost"
  },
  "devDependencies": {
    "@chainlink/contracts": "^0.4.2",
    "@nomiclabs/hardhat-ethers": "2.2.3",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.0.3",
    "ethers": "5.8.0",
    "hardhat": "^2.21.0"
  }
}
